package dev.austinbarnes.retailinventorymanagement.common;

/**
 * FilterDTO is an interface that requires implementing classes to provide a BaseFilterDTO.
 */
public interface FilterDTO {
    BaseFilterDTO baseFilterDTO();
}
